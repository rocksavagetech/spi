/*
package tech.rocksavage.chiselware.SPI

import chisel3._
import chisel3.util._

class SPIBase(p: BaseParams) extends Module {
  if (p.spiMaster) Some(Module(new SPIMaster(p))) else None
  // if (!p.spiMaster) Some(Module(new SPISlave(p))) else None
}
 */
